# CRUD Student API Example
* Init database from file(SQLite3 needed): `sqlite3 database.db < dump.sql`
* Install required dependency: `pip install -r requirements.txt`
* Run server: `python student_api.py`